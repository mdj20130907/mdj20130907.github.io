// You indicate whether a particular String can be modified (or mutated) by assigning it to a variable (in which case it can be modified),
// or to a constant (in which case it can’t be modified)

var s1 = "hi"
s1 += " there"

let s2 = "constant string"
// s2 += "!"
