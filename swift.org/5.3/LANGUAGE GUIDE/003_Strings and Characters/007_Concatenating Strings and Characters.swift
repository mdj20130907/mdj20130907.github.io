// addition operator (+)
let s1 = "hello"
let s2 = " there"
var welcome = s1 + s2
print(welcome)

// addition assignment operator (+=)
var instruction = "look over"
instruction += s2
print(instruction)

let c1:Character = "!"
welcome.append(c1)// 字符串或者字符都可以
print(welcome)
